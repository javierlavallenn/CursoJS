// Definiendo una Funcion (no se ejecuta inmediatamente)
function sumar () {
    let resultado = 1 + 20;
    console.log(resultado);
}

// Otro ejemplo
function multiplicar (a,b) {
    let resultado = a * b;
    console.log(resultado);
}
multiplicar(2, 10)